<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Building Charge Calculator</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="container">
        <!-- App Name Visible Here -->
        <h1>Building Charge Calculator</h1> <!-- This should show at the top of the page -->
        
        <form action="/calculate" method="POST">
            <label>Local Body:</label>
            <select name="LocalBody" required>
                <option value="1">CM</option>
                <option value="2">TP</option>
                <option value="3">VP</option>
            </select>

            <label>Usage:</label>
            <select name="Usage" required>
                <option value="1">Residential</option>
                <option value="2">Commercial</option>
                <option value="3">Industrial</option>
                <option value="4">Institutional</option>
            </select>

            <label>High Rise Building:</label>
            <select name="isHRise" required>
                <option value="1">Yes</option>
                <option value="2">No</option>
            </select>

            <label>MSB:</label>
            <select name="MSB" required>
                <option value="1">OMSB</option>
                <option value="2">MSBR</option>
                <option value="3">MSB</option>
            </select>

            <label>Site Area (sqm):</label>
            <input type="number" step="0.01" name="SiteArea" required>

            <label>Built Area (sqm):</label>
            <input type="number" step="0.01" name="BuiltArea" required>

            <label>FSI:</label>
            <input type="number" step="0.01" name="FSI" required>

            <label>GV:</label>
            <input type="number" step="0.01" name="GV" required>

            <button type="submit">Calculate</button>
        </form>

        <!-- Only show results if they exist -->
        <% if (result) { %>
            <h2>Results</h2>
            <p>Scrutiny Fee: Rs. <%= result.scrutinyFee %></p>
            <p>I&A Charges: Rs. <%= result.iaCharges %></p>
            <p>Security Deposit: Rs. <%= result.securityDeposit %></p>
            <p>Shelter Fund: Rs. <%= result.shelterFund %></p>
            <p>Premium FSI: Rs. <%= result.premiumFSI %></p>
            <p>OSR: Rs. <%= result.osr %></p>
            <p>Development Charges Land: Rs. <%= result.devChargeL %></p>
            <p>Development Charges Building: Rs. <%= result.devChargeB %></p>
            <p>CC Charge: Rs. <%= result.ccCharge %></p>
        <% } %>
    </div>
</body>
</html>
